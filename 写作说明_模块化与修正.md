# 写作说明：模块化 do 文件、修正点与结果解读

> 配套：`do_files/`（拆分后的可分别运行的 do 文件）+ `减污降碳_完整实证结果_整合稿.docx`（全部实证结果三线表 + 脚注）。
> 数据：`data_spatial_v2.dta`（289 城市 × 2003–2023）。

---

## 一、do 文件已拆分为模块（在 `do_files/` 文件夹，各自可独立运行）

| 文件 | 内容 | 主要输出 |
|---|---|---|
| `00_setup.do` | 公共数据准备：派生变量、全局宏、保存 `results/_work.dta` | 工作副本 |
| `01_descriptive.do` | 描述性统计（表1） | 结果01_描述性统计.rtf |
| `02_baseline.do` | 基准 FE 阶梯（表2）+ 平行趋势 + 安慰剂 + PSM-DID | 结果02_*.rtf、图 |
| `03_robustness.do` | SEM 口径（表3）/ Tapio（表4）/ 剔除政策（表5）/ 4A·IV（表6） | 结果03_*.rtf |
| `04_mechanism.do` | 五条机制三步法（表7） | 结果04_机制_*.rtf |
| `05_heterogeneity.do` | 11 维异质性，每维单独成表（表8） | 结果05_异质性_*.rtf |
| `06_spatial.do` | 权重/Moran/SDM/距离衰减/空间DID/LISA（表9–14） | 结果06_*.rtf、图 |
| `run_all.do` | 主控：一次跑全部 | — |

**运行方式**：把 `do_files/` 里的文件与 `data_spatial_v2.dta` 放同一目录，`cd` 过去后：
- 只想跑某部分 → 直接 `do "03_robustness.do"`（每个模块开头会自动 `do "00_setup.do"`，无需手动准备数据）；
- 全部跑 → `do "run_all.do"`。
- 每张正式表都在对应 `.rtf` 下方附有**【方法】【计算过程】【经济含义】【参考文献】**四段脚注，符合投稿标注规范。
- 空间模块需 `ssc install xsmle`，耗时较长，建议单独跑。

---

## 二、本轮的关键修正（对应你逐条的问题）

### 表2（基准）——每个固定效应含义已写进脚注
城市FE、年份FE、区域×年FE、省份×年FE、城市线性趋势逐一控制，脚注逐一解释其吸收的混淆源，并给出文献（Callaway & Sant'Anna 2021；Sun & Abraham 2021）。

### 表3（SEM）——**已修正，现在全部显著** ✅
- **问题根源**：之前的 SEM 用 `poll_idx × co2_wt` 重构，但**这两列并不是构造 `lnpoco2` 的原始数据**（相关系数仅 0.31，回归系数 0.19 与 0.44 而非 1 与 1）。
- **真相**：按 0630 文档，`lnpoco2 = ln(环境污染指数EPI[改进熵权-TOPSIS:废水/SO₂/烟尘] × CO₂)`，它**本身就是 SEM=EPI×CO₂ 交乘法**。
- **修正**：以综合指标 `lnpoco2` 及其标准化/单位GDP强度/缩尾等 5 种变换作 SEM 延伸——**五列系数均 1% 显著为负（−0.057～−0.183）**。方向核实无误（越小越好，5A 使其下降）。

### 表4（Tapio）——完整表格 + 模型含义已注明
脱钩弹性 e=Δln(减污降碳)/Δln(人均GDP)；e<0 且经济增长=强脱钩。5A 显著提升强脱钩概率（处理组 48.5% vs 对照组 43.7%）。表下注明模型含义与脱钩状态分类。

### 表5（同期政策）——**已修正为“剔除政策城市样本”** ✅
- **问题**：之前是“加政策虚拟变量”，DID 几乎不变（−0.103 全同），你正确地觉得可疑。
- **修正**：改为**剔除**智慧城市 / 节能减排(碳披露) / 旅游枢纽城市样本后重估，系数**确有差异**：−0.094 / −0.093 / −0.089（同时剔除 −0.078），N 也随之下降，方向一致但量级不同，逻辑正确。

### 表6（4A 与 IV）——已明确各列含义
- **“4A 替换”= 列(2)**：把核心解释变量由 5A 政策(DID) 换成 **4A 政策(DID4A)**，看 4A 自身有没有效应（结果：方向一致但偏弱，因 4A 是次级品牌且时点数据仅 1/3）。
- **“5A 控 4A”= 列(3)**：回归里**同时放入 5A(DID) 与 4A 强度(ln4A)**，即在**控制一个城市 4A 景区数量**的前提下看 5A 的净效应（结果：5A 仍显著 → 效应特定于顶级 5A，而非“有景区就行”）。

### 表7（机制）——格式对标参考文档
三步法三列式（第一步 lnpoco2 / 第二步 中介 / 第三步 lnpoco2），系数上、(t值)下，含常数项与控制变量行。五条机制（含新增“公众环境关注度”）Sobel 均显著。

### 表8（异质性）——每个分组单独成表、完整结果
11 个维度各自一张完整两列表（含全部控制变量 + 组间系数差异 p 值），对标 0630 文档范式。★为新增创新维度（旅游资源丰度/距省会/海陆/数字科技）。

### 表9–14（空间）——完整结果 + 经济含义脚注；LISA 已并入空间部分
空间诊断、SDM 三 FE（完整系数，取自你 Stata 运行结果）、五矩阵、距离衰减、**LISA 空间异质性**、控制组溢出污染，均在表下加“经济含义”脚注。

---

## 三、结果的经济含义速览（可直接写进正文）

- **基准**：5A 使城市“污染×碳”协同指数下降约 10%，逐级收紧固定效应后稳健。
- **SEM/口径**：换任何标准化方式结论不变——效应是“协同下降”而非度量假象。
- **脱钩**：5A 推动城市从“增长即排放”转向“增长且减污降碳”的强脱钩。
- **政策**：剔除智慧城市/节能减排/旅游枢纽后效应仍在，非同期政策驱动。
- **识别**：4A 弱、5A 控 4A 仍强、IV 同号显著 → 效应特定于顶级 5A 且不受内生性反转。
- **机制**：产业结构高级化、能源强度下降、绿色创新、环境规制、公众关注五条渠道共同传导。
- **异质性**：自然类 5A、旅游资源丰富、远离省会、沿海、数字科技强的城市效应更大。
- **空间**：减污降碳正向空间集聚（ρ>0）；溢出半径约 300–400km；LL 洁净集聚区政策最见效。

---

## 四、仍需你确认/提供

1. **政策名单**（智慧城市/节能减排/旅游枢纽）为主流口径近似，请对照官方文件在 `00_setup.do` 中增删。
2. **SEM 若想用“分项熵权重建”**（而非综合指标变换），需提供构造 `lnpoco2` 的**分项原始序列**（废水/SO₂/烟尘/CO₂），我可补熵权-TOPSIS 重建代码。
3. 空间 SDM 完整系数表取自你此前 Stata 运行（DOC1）；`06_spatial.do` 可在你本机重新生成并覆盖。
